name: Mega Daily Generate

on:
  schedule:
    - cron: "15 10 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          pip install pandas numpy requests

      - name: Ingest + Features + Generate
        run: |
          python core/ingest_megasena.py
          python core/features_megasena.py
          python core/generator.py

      - name: Commit artifacts (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add out/jogos_gerados.json
          git add data/results/megasena.csv
          git add data/features/dezenas.csv

          # adiciona histórico só se existir (evita erro pathspec)
          if [ -d "out/history" ]; then
            git add out/history
          fi

          git commit -m "chore: update mega artifacts" || echo "no changes"
          git push
